# Rails backend the PWA talks to.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Google OAuth client ID — used once the real zkLogin flow lands
# (currently the sign-in surface is a stub; see lib/auth.ts).
# NEXT_PUBLIC_GOOGLE_OAUTH_CLIENT_ID=
